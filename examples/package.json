{
  "description": "A set of examples for the wallet-monitor package",
  "scripts": {
    "wallet-watcher": "ts-node ./wallet-watcher.ts",
    "wallet-exporter": "ts-node ./wallet-exporter.ts",
    "multi-wallet-watcher": "ts-node ./multi-wallet-watcher.ts",
    "multi-wallet-exporter": "ts-node ./multi-wallet-exporter.ts"
  },
  "dependencies": {
    "wallet-monitor": "file:../",
    "winston": "3.8.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
